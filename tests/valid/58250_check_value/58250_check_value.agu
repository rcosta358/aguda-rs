-- Author: 58250, Leonardo Monteiro

-- Function to check if a number is positive or negative
let check_sign(n) : Int -> String = 
  if n > 0 then "Positive" else "Negative"

let result : String = check_sign(-5)

let _ : Unit = 
    print(result)  -- Expected result: Negative
