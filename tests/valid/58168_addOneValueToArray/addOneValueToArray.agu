-- Author: 58168, Henrique Costa Vale

f(a,b) : (Int [] , Int) -> Bool =
  let value : Int = false; 
  let c Int [] = new Int[length(a) + 1 | 0];
  let i : Int = 0;
  while i < length(c) do (
	if i < length(a) then set c[i] = a[i] else set c[i] = b;
	set i = i + 1;
  )
  set result = true;
  result

let _ : Unit =
  print(f([1,2,3,4] , 5))