-- Author: 56332, Student Ricardo Sobral

--imprime todos os numeros até 0 (incluido)
let decToZero (n) : Int -> Unit =
    if n == -1 then unit
    else (
        print(n);
        decToZero(n - 1)
    )
let _ : Unit = decToZero(5)