-- Author: 58235, António Almeida

let d : Int = 3
let w : Int = 4
let h : Int = 2

let createCube (w, h, d) : Int -> Int -> Int -> Int [][][] =
    let cube : Int[][][] = new Int[][] [d | new Int[] [h | new Int [w | 0]]];
    cube

let printCube(c) : Int[][][] -> Unit =
    let i : Int = 0 ;

    while i < length(a) do (
        let j : Int = 0;

        while j < length(a[0]) do (
            let k : Int = 0;

            while k < length(a[0][0]) do (
                print(a[i][j][k]); print(" ");
                set k = k + 1
            );

            print("\n");
            set j = j + 1
        );

        print("\n") ; print("\n");  
        set i = i + 1
    )

let _ : Unit = 
    printCube(createCube(w, h, d))