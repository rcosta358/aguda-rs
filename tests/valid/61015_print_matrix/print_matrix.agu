-- Author: 61015, Filipe Lopes

let matrix : Int[][] = new Int[] [2 | new Int [2 | 0]];
let i : Int = 0;
while i < length(matrix) do (
  let j : Int = 0;
  while j < length(matrix[0]) do (
    print(matrix[i][j]); print(" ");
    set j = j + 1
  );
  print("\n");
  set i = i + 1
);